services:
  postgres:
    image: postgres:17
    container_name: grafana-clone-postgres
    restart: unless-stopped
    shm_size: "1gb"
    environment:
      POSTGRES_DB: grafana_clone
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    command:
      - postgres
      - -c
      - shared_preload_libraries=pg_stat_statements
      - -c
      - pg_stat_statements.track=all
      - -c
      - track_io_timing=on
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d grafana_clone"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
